<%= form_for(current_user.attendances.find_by(attended_id: @event.id), 
            html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Will not attend", class: "btn" %>            
<% end %>